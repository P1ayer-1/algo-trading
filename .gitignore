.env
__pycache__/
.vscode/
node_modules/
*.egg-info/
.pytest_cache/

# Recorded feature data. These grow fast (~350k rows/day at the default
# 250ms sample interval) and are machine-specific training data, not source.
data/
*.csv

# Trained models. Regenerable from data/ with analysis/train_model.py, and the
# metadata.json alongside each records exactly how (features, params, seed).
models/
